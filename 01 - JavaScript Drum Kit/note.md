1. 使用 data-key 來儲存自訂資料
2. data-key 的 selector 語法是 [data-key="XXX"]
3. 使用 querySelector 取出的元素是 Elemnet 實例
4. audio 用 currentTime 控制當前播放時間點; 用 play() 開始播放
5. Element 實例的 classList 屬性可以控制 class
6. 使用 early return 讓程式碼更清晰
7. transitionend 事件會在動畫 transition 結束後觸發
8. addEventListener callback function 的 this 會是事件監聽器添加的對象